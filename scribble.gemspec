Gem::Specification.new do |s|
  s.name = 'colorize'
  s.version = Colorize::VERSION
  s.required_ruby_version = '>= 2.6'

  s.authors = ['ScribbleLabApp']
  s.email = 'scribblelabapp.dev@gmail.com'

  s.homepage = 'http://github.com/ScribbleLabApp/SCRIBBLE-LLDB-CLI'
  s.description = 'ScribbleLabs official cli tool.'
  s.summary = 'ScribbleLabs official cli lldb tool. Required for plug-in development.'
  s.license = 'Apache 2.0'

  s.require_paths = ['lib']

  s.files = [
    # files
  ]
end